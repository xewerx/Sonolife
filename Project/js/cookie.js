const back = document.querySelector(".buyButton");
const logo = document.querySelector(".logo");

back.addEventListener("click", () => window.location = 'index.html')
logo.addEventListener("click", () => window.location = 'index.html')